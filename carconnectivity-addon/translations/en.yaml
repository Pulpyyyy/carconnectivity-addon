# The only "option" of the edge addon: a translated pointer to the real
# configuration (the in-app page). Its value is never read.
configuration:
  info:
    name: ⚙️ Configuration has moved to the Web UI
    description: 'This add-on is configured graphically. Path: the add-on "Info" tab → "OPEN WEB UI" button → "Configuration" button in the top bar of the page. This field is ignored.'
