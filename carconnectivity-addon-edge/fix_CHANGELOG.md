- **Seamless upgrade from pre-configui versions**: at startup, when the page config does not exist yet, a configuration generated by a previous version of the add-on (`carconnectivity.UI.json`) is now used directly, so the add-on keeps running after the upgrade instead of starting with defaults (#151)
- **Configuration page**: the "restart required" notice is now persistent (it survives switching to the dashboard and back) and links straight to the add-on page in Home Assistant, where the Restart and Logs buttons live; leaving the page with unsaved changes now asks for confirmation (#151)
- **EU Data Act read-only warning**: shown only when at least one vehicle actually uses the EU Data Act source, instead of permanently (#151)
