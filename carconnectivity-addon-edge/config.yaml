name: "CarConnectivity Add-on Edge"
version: 0.8.7
slug: "carconnectivity-addon-edge"
description: "⚠️EDGE VERSION⚠️ CarConnectivity Add-on Edge, may be unstable !"
image: "docker.io/pulpyyyy/carconnectivity-addon-edge-{arch}"
panel_icon: "mdi:car-wireless"
panel_title: "CarConnectivity"
map:
  - addon_config:rw
arch:
  - aarch64
  - amd64
ingress: true
init: false
hassio_api: true
hassio_role: homeassistant
homeassistant_api: true
# Configuration is done entirely in the addon Web UI (Ingress → /configui/),
# so the addon exposes no Home Assistant options here.
options: {}
schema: {}
